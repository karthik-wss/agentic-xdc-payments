{
  "name": "xdc-multi-token-ai-agent",
  "version": "2.0.0",
  "description": "AI agent for XDC Network: send native XDC / ERC-20 tokens and swap on a DEX via natural language",
  "type": "module",
  "main": "src/agent.js",
  "scripts": {
    "start": "node src/agent.js",
    "dev": "node --watch src/agent.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "ws": "^8.21.0"
  }
}
